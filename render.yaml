services:
  # Backend: FastAPI Web Service
  - type: web
    name: vaayuu-backend
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    # Reduced to 2 workers for memory stability on free tier
    startCommand: gunicorn api.main:app -w 2 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:$PORT
    envVars:
      - key: PORT
        value: 10000
      - key: RENDER
        value: true
      - key: BLYNK_AUTH_TOKEN
        sync: false
      - key: BLYNK_EVENT_CODE
        value: critical_alert
      - key: SMS_COOLDOWN_SECONDS
        value: 300
      - key: VITE_GEMINI_API_KEY
        sync: false
